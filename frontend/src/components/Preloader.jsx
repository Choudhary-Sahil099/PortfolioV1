import React from 'react'

const Preloader = () => {
  return (
    <div id='preloader'> 
    {/* dont remove it as it has the id of the the preLoader predefined in the css properties */}
    </div>
  )
}

export default Preloader
